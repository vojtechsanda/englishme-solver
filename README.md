# Englishme-solver
```javascript

// Solver for words type
ex.words.words.forEach((w,i) => {
    w.use_count = 1;
    w.use_wrong = 0;
    w.use_level = 2;
    w.use_last_round = (i+1);
})
abcd();
$(location).attr('href', ex.getLinkComplete());

```